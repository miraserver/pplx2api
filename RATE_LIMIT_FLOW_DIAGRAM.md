# Rate Limit Flow Diagrams

## Current Request Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Client HTTP Request                           │
│              POST /v1/chat/completions                           │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│              service.ChatCompletionsHandler()                    │
│                                                                  │
│  1. Parse request body                                          │
│  2. Extract messages, model, images                             │
│  3. Get initial session index                                   │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│               config.Sr.NextIndex()                              │
│                                                                  │
│  • Mutex.Lock()                                                 │
│  • index = sr.Index                                             │
│  • sr.Index = (index + 1) % len(Sessions)  [Round-robin]       │
│  • Mutex.Unlock()                                               │
│  • return index                                                 │
└────────────────────────┬────────────────────────────────────────┘
                         │
                         ▼
┌─────────────────────────────────────────────────────────────────┐
│                    Retry Loop                                    │
│             for i := 0; i < RetryCount; i++                      │
│                                                                  │
│  ┌────────────────────────────────────────────────────┐         │
│  │  index = (index + 1) % len(Sessions)              │         │
│  │  session = GetSessionForModel(index)              │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│  ┌────────────────────────────────────────────────────┐         │
│  │  pplxClient = NewClient(session.SessionKey, ...)  │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│  ┌────────────────────────────────────────────────────┐         │
│  │  Upload images (if any)                           │         │
│  │    ↓ Error? → continue to next session           │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│  ┌────────────────────────────────────────────────────┐         │
│  │  Upload text as file (if too long)                │         │
│  │    ↓ Error? → continue to next session           │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│  ┌────────────────────────────────────────────────────┐         │
│  │  SendMessage() to Perplexity API                  │         │
│  │    ↓                                              │         │
│  │  POST perplexity.ai/rest/sse/perplexity_ask      │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│  ┌────────────────────────────────────────────────────┐         │
│  │  Check Response Status                            │         │
│  │                                                   │         │
│  │  • 429? → return "rate limit exceeded"           │         │
│  │  • 200? → stream response to client              │         │
│  │  • Other error? → return error                   │         │
│  └────────────────────┬───────────────────────────────┘         │
│                       │                                         │
│                       ▼                                         │
│               Success? Return                                   │
│               Error? Continue to next session                   │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
                         │
                         ▼
              Success: Stream to client
              All failed: Return 500 error
```

---

## Current 429 Handling (Problem!)

```
Request N: Use Account A
    ↓
    POST to Perplexity API
    ↓
    ❌ 429 Rate Limited!
    ↓
    Switch to Account B
    ↓
    POST to Perplexity API
    ↓
    ✅ Success
    ↓
Return to client

Request N+1: Use Account A again! ⚠️
    ↓
    POST to Perplexity API
    ↓
    ❌ 429 Rate Limited AGAIN!
    ↓
    Switch to Account B
    ↓
    POST to Perplexity API  
    ↓
    ✅ Success

Request N+2: Use Account A AGAIN! ⚠️⚠️
    ↓
    ... (repeats the problem)
```

**Issue**: No memory of rate-limited accounts!

---

## Proposed Improved Flow

```
Request N: Select Session
    ↓
┌───────────────────────────────────────┐
│   For each session in rotation:       │
│                                        │
│   1. Is session rate-limited?         │
│      └─ Yes → Check expiry time       │
│         └─ Still limited? → Skip      │
│         └─ Expired? → Mark available  │
│      └─ No → Use this session         │
│                                        │
│   2. Is session healthy?              │
│      └─ Check error rate              │
│      └─ Check circuit breaker         │
│                                        │
│   3. Select best available session    │
└───────────────┬───────────────────────┘
                │
                ▼
    Use selected healthy session
                ↓
    POST to Perplexity API
                ↓
         Response Status?
                ↓
    ┌───────────┴───────────┐
    │                       │
    ▼                       ▼
  429 Error              Success
    │                       │
    ▼                       │
┌─────────────────┐         │
│ Parse Retry-After│         │
│ header           │         │
│                  │         │
│ Set cooldown:    │         │
│ • IsRateLimited  │         │
│ • Expiry time    │         │
│ • Inc error count│         │
└────────┬─────────┘         │
         │                   │
         ▼                   ▼
    Try next           Update stats:
    healthy session    • Success count
         │             • Last used time
         │             • Reset error count
         │                   │
         └──────────┬────────┘
                    ▼
              Return to client
```

---

## Background Cookie Refresh Job

```
┌──────────────────────────────────────────────────────────────┐
│                      main.go                                  │
│                                                               │
│  sessionUpdater = GetSessionUpdater(24 * time.Hour)          │
│  sessionUpdater.Start()                                      │
└─────────────────────────┬────────────────────────────────────┘
                          │
                          ▼
┌──────────────────────────────────────────────────────────────┐
│                job.SessionUpdater                             │
│                                                               │
│  Every 24 hours:                                             │
│    ┌──────────────────────────────────────────┐             │
│    │  For each session in parallel:           │             │
│    │    1. GET perplexity.ai/api/auth/session │             │
│    │    2. Extract new cookie value           │             │
│    │    3. Update session token               │             │
│    └──────────────────┬───────────────────────┘             │
│                       │                                       │
│                       ▼                                       │
│    ┌──────────────────────────────────────────┐             │
│    │  Save to sessions.json:                  │             │
│    │  {                                       │             │
│    │    "sessions": [                         │             │
│    │      {"session_key": "new_token_1"},     │             │
│    │      {"session_key": "new_token_2"}      │             │
│    │    ]                                     │             │
│    │  }                                       │             │
│    └──────────────────────────────────────────┘             │
│                                                               │
└───────────────────────────────────────────────────────────────┘

Note: Cookie refresh currently does NOT preserve rate limit state!
      If account is rate-limited and cookie refreshes, state is lost.
```

---

## Account State Transitions (Proposed)

```
┌─────────────┐
│   HEALTHY   │ ← Default state for new accounts
└──────┬──────┘
       │
       │ Multiple requests
       ▼
┌─────────────┐
│    ACTIVE   │ ← Normal operation
└──────┬──────┘
       │
       │ Success responses
       │ increment success count
       │
       ├─────────────────────┐
       │                     │
       │ 429 error           │ Other errors
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│ RATE_LIMITED │      │   DEGRADED   │
│              │      │              │
│ • Skip for   │      │ • Error rate │
│   cooldown   │      │   tracking   │
│ • Expiry set │      │ • May skip   │
└──────┬───────┘      └──────┬───────┘
       │                     │
       │ Cooldown            │ Too many
       │ expires             │ errors
       │                     │
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│  RECOVERING  │      │  CIRCUIT_OPEN│
│              │      │              │
│ • Try once   │      │ • Don't use  │
│ • If success │      │ • Auto-reset │
│   → HEALTHY  │      │   after time │
└──────────────┘      └──────┬───────┘
       │                     │
       │ Success             │ Timeout
       ▼                     ▼
┌──────────────┐      ┌──────────────┐
│   HEALTHY    │      │ HALF_OPEN    │
└──────────────┘      └──────────────┘
                             │
                             │ Try once
                             │
                      ┌──────┴──────┐
                      │             │
                  Success       Failure
                      │             │
                      ▼             ▼
                  HEALTHY    CIRCUIT_OPEN
```

---

## Data Flow: Configuration to Request

```
┌─────────────────────────────────────────────────────────────┐
│                    Environment Variables                     │
│  SESSIONS=token1,token2,token3                              │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Load on startup
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              config.LoadConfig()                             │
│                                                              │
│  parseSessionEnv() splits by comma                          │
│    ↓                                                         │
│  []SessionInfo{                                             │
│    {SessionKey: "token1"},                                  │
│    {SessionKey: "token2"},                                  │
│    {SessionKey: "token3"}                                   │
│  }                                                          │
│    ↓                                                         │
│  ConfigInstance.Sessions = sessions                         │
│  ConfigInstance.RetryCount = len(sessions)                  │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Also load from file if exists
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              sessions.json (persisted state)                 │
│  {                                                           │
│    "sessions": [                                            │
│      {"session_key": "refreshed_token1"},                   │
│      {"session_key": "refreshed_token2"}                    │
│    ]                                                        │
│  }                                                          │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ On incoming request
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              SessionRagen.NextIndex()                        │
│                                                              │
│  Current State:                                             │
│    Index: 1                                                 │
│    Sessions: [token1, token2, token3]                       │
│                                                              │
│  Returns: 1                                                 │
│  Updates: Index = 2                                         │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Pass to handler
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              GetSessionForModel(index)                       │
│                                                              │
│  RLock()                                                    │
│  session = Sessions[1]  // Returns token2                   │
│  RUnlock()                                                  │
│  return session                                             │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Create client
                         ▼
┌─────────────────────────────────────────────────────────────┐
│              core.NewClient()                                │
│                                                              │
│  • Set cookie: __Secure-next-auth.session-token=token2     │
│  • Configure HTTP client                                    │
│  • Return Client instance                                   │
└────────────────────────┬────────────────────────────────────┘
                         │
                         │ Make request
                         ▼
                  Perplexity API
```

---

## Session State After Phase 1 Implementation

```
┌────────────────────────────────────────────────────────────────┐
│                      SessionInfo                               │
├────────────────────────────────────────────────────────────────┤
│  SessionKey        string         "eyJhbGci..."                │
│  IsRateLimited     bool           false                        │
│  RateLimitExpiry   time.Time      2024-11-01 15:30:00         │
│  mutex             sync.RWMutex   (internal locking)           │
└────────────────────────────────────────────────────────────────┘
                                │
                                │ Methods
                                ▼
┌────────────────────────────────────────────────────────────────┐
│  SetRateLimited(duration)                                      │
│    • Lock()                                                    │
│    • IsRateLimited = true                                      │
│    • RateLimitExpiry = Now + duration                          │
│    • Unlock()                                                  │
└────────────────────────────────────────────────────────────────┘
┌────────────────────────────────────────────────────────────────┐
│  IsAvailable() bool                                            │
│    • RLock()                                                   │
│    • If IsRateLimited && Now < Expiry: return false           │
│    • If IsRateLimited && Now >= Expiry:                        │
│        - Upgrade to Lock()                                     │
│        - IsRateLimited = false                                 │
│    • RUnlock()                                                 │
│    • return true                                               │
└────────────────────────────────────────────────────────────────┘
```

---

## Comparison: Before vs After

### Before (Current)

```
Request Flow:
  User → Handler → Round-robin → Session A → 429 → Session B → Success
  User → Handler → Round-robin → Session A → 429 → Session B → Success
  User → Handler → Round-robin → Session A → 429 → Session B → Success
                                  ⬆ Keeps retrying rate-limited account!

Retries per request: 2+ (wasted)
Error rate: High
User latency: High (multiple failed attempts)
```

### After Phase 1 (With Cooldown)

```
Request Flow:
  User → Handler → Health check → Session A → 429 → Mark limited (60s)
                → Health check → Session B → Success
  User → Handler → Health check → Skip A (limited) → Session B → Success
  User → Handler → Health check → Skip A (limited) → Session B → Success
  ... 60 seconds later ...
  User → Handler → Health check → Session A (recovered) → Success

Retries per request: 1-2 (optimal)
Error rate: Low
User latency: Low (fast failover to healthy accounts)
```

### After Phase 2 (With Health Tracking)

```
Session States:
  ┌─────────┬──────────────┬────────────┬──────────┐
  │ Session │ Health       │ Last Error │ Priority │
  ├─────────┼──────────────┼────────────┼──────────┤
  │ A       │ HEALTHY      │ None       │ High     │
  │ B       │ RATE_LIMITED │ 429 (30s)  │ Skip     │
  │ C       │ DEGRADED     │ 500 (10s)  │ Low      │
  └─────────┴──────────────┴────────────┴──────────┘

Request Flow:
  User → Handler → Select best session (A) → Success
  User → Handler → Select best session (A) → Success
  User → Handler → Select best session (A) → 429 → Mark A limited
                → Select best session (C) → Success (despite DEGRADED)

Smart selection based on health metrics!
```

---

## Metrics & Observability (Phase 3)

```
┌──────────────────────────────────────────────────────────────┐
│                   Metrics Collection                          │
│                                                               │
│  Per Session:                                                │
│    • Total Requests                                          │
│    • Success Count                                           │
│    • Error Count (by type: 429, 500, network)               │
│    • Current State (healthy, rate_limited, etc.)            │
│    • Last Used Timestamp                                     │
│    • Average Response Time                                   │
│                                                               │
│  Global:                                                     │
│    • Total Requests                                          │
│    • Rate Limit Events                                       │
│    • Retry Count                                             │
│    • Failover Count                                          │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ Export
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                  Prometheus Endpoint                          │
│                  GET /metrics                                 │
│                                                               │
│  pplx_session_requests_total{session="A"} 1523               │
│  pplx_session_errors_total{session="A",type="429"} 23        │
│  pplx_session_state{session="A",state="healthy"} 1           │
│  pplx_session_state{session="B",state="rate_limited"} 1      │
│  pplx_rate_limit_events_total 45                             │
└────────────────────────┬─────────────────────────────────────┘
                         │
                         │ Scrape
                         ▼
┌──────────────────────────────────────────────────────────────┐
│                     Grafana Dashboard                         │
│                                                               │
│  ┌─────────────────────────────────────────────────┐        │
│  │  Session Health Status                           │        │
│  │  🟢 Session A: HEALTHY (1234 req, 99.8% success) │        │
│  │  🔴 Session B: RATE_LIMITED (expires in 45s)    │        │
│  │  🟡 Session C: DEGRADED (89% success rate)      │        │
│  └─────────────────────────────────────────────────┘        │
│                                                               │
│  ┌─────────────────────────────────────────────────┐        │
│  │  Rate Limit Events (last 24h)                   │        │
│  │  [Graph showing spikes]                          │        │
│  └─────────────────────────────────────────────────┘        │
│                                                               │
│  ┌─────────────────────────────────────────────────┐        │
│  │  Request Distribution                            │        │
│  │  Session A: ████████████████████░░ 75%          │        │
│  │  Session B: ░░░░░░░░░░░░░░░░░░░░░░ 0%           │        │
│  │  Session C: ████████░░░░░░░░░░░░░░ 25%          │        │
│  └─────────────────────────────────────────────────┘        │
└───────────────────────────────────────────────────────────────┘
```

---

**Document Version**: 1.0  
**Last Updated**: 2024-11-01
